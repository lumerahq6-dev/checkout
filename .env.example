# Paddle.js (Billing) — https://developer.paddle.com/paddlejs/client-side-tokens
# Create tokens in Paddle Dashboard → Developer tools → Authentication
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=
# Omit or set to "sandbox" for sandbox (test_… tokens). Live uses live tokens; do not set sandbox for production.
NEXT_PUBLIC_PADDLE_ENV=
